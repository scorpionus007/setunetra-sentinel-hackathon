# api/ — Aryan

Owns features **#11 Central registry & camera onboarding RBAC**,
**#6 Dept-wise/area RBAC**, **#2 Storage tiering policy**, **#15
VAHAN/SARTHI integration adapters**. Supports **#8 compliance scoring**
(data side), **#12 gap analysis** (query logic), **#16 route assembly**.

## Run

    pip install -r requirements.txt
    uvicorn app.main:app --reload
